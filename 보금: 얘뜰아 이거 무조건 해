git config --global user.name "본인 GitHub 아이디"
git config --global user.email "GitHub에 등록된 이메일"

git checkout main
git pull --rebase origin main

git commit --allow-empty -m "Contribution test"
git push origin main



만약 git push가 거부되면:

git pull --rebase origin main
git push origin main
